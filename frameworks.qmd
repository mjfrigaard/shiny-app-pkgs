# Frameworks {#sec-frameworks .unnumbered}

```{r}
#| eval: true 
#| echo: false 
#| include: false
source("_common.R")
```

```{r}
#| label: co_box_dev
#| echo: false
#| results: asis
#| eval: true
co_box(
  color = "r", 
  look = "minimal",
  header = "Alert",
  contents = "The contents for section are being development. Thank you for your patience."
)
```

### Shiny frameworks

This section covers three popular Shiny frameworks:

1. `gap`: a [`golem`](https://thinkr-open.github.io/golem/) app-package

2. `lap`: a [`leprechaun`](https://leprechaun.opifex.org/#/) app-package

3. `rap`: a [`rhino`](https://appsilon.github.io/rhino/) app (pseudo-package)[^rhino-non-pkg]

[^rhino-non-pkg]: `rhino` apps aren't R packages--they're dependencies are managed with a `dependencies.R` file and [`box`](https://klmr.me/box/) modules.
